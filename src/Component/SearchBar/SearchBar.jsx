import React from 'react'

const SearchBar = ({data}) => {
  
  return (
    <>
     {/* {
       data.map((item) => (
         <div key={item.id}>
           <h3>{item}</h3>
         </div>
       ))
     } */}
    </>
  )
}

export default SearchBar